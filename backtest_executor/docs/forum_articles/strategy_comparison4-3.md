# ETF轮动策略研究(四之三)：动态池策略的参数回测与结果分析

上一篇介绍了 `backtest_executor` 的基本设计：把策略参数、回测区间、搜索轮次和结果记录统一放进一套配置化流程里。本文接着用一个实际案例说明这套流程怎么操作。

本次测试的策略程序是 `ETF_7star_opt_dynamic`，回测在 JQ 平台上执行，区间为 2023-01-01 到 2026-03-01，初始资金为 100000。

这篇文章主要做三件事：

1. 简要介绍七星动态池策略的核心逻辑。
2. 展示通过 YAML 配置跑出的几轮代表性回测结果。
3. 分析哪些参数方向有效，哪些结果更适合作为后续验证版本。

## 策略简介

`ETF_7star_opt_dynamic` 是一个动态 ETF 池版本的轮动策略。它和固定池策略最大的区别在于：候选 ETF 不是人工预先写死一组，而是在运行中从全市场 ETF 里按规则筛选出来。

策略主要由三层逻辑组成。

### 1. 动态池构建

每天 09:00 会检查是否需要刷新 ETF 池。默认每 20 个交易日刷新一次。

动态池筛选流程大致如下：

1. 扫描全市场 ETF。
2. 按名称关键词过滤 A 股、跨境/QDII、债基/货币类 ETF。
3. 排除上市不足 60 天的 ETF。
4. 要求过去 20 日日均成交额不低于 5000 万。
5. 对追踪同一指数的 ETF 做去重，保留成交额较大的品种。
6. 对部分白名单 ETF 做保留。

这套设计的目标是让策略不依赖固定池，而是自动寻找当前市场中流动性足够、同质化较低的 ETF。

池子确定后，策略会对候选 ETF 做 25 日加权线性回归评分，与前面的策略一致。

### 2. 过滤条件

在动量排序之外，策略还支持多类过滤器：

- `S`：动量得分区间。
- `st`：短期动量过滤。
- `r`：R2 过滤。
- `ar`：年化收益率过滤。
- `ma`：均线过滤。
- `v`：成交量过滤。
- `ls`：近三日单日跌幅过滤。
- `fm`：动态池市场类型过滤，例如过滤 A 股、过滤 QDII、过滤债基货币。

这些过滤器通过 `EXECUTION_` 参数暴露给配置文件，由 `backtest_executor` 在每轮回测前注入。

### 3. 止损和冷却

策略还内置了几类风控：

- 固定比例止损（`fl`）。
- 当日跌幅止损。
- ATR 动态止损。
- 卖出后冷却期（`slday`）。
- 冷却期避险 ETF。

本次配置重点测试的是固定止损 `fl`、近三日跌幅过滤 `ls`、卖出冷却 `slday` 等参数组合。

## 回测配置

主要参数空间如下：

| 参数 | 对应策略变量 | 含义 | 候选方向 |
|---|---|---|---|
| `S` | `EXECUTION_SCORE_THRESHOLD` | 动量得分区间 | `[0,4]`、`[0,5]`、`[0,6]`、`[0,8]`、`[0,10]` |
| `st` | `EXECUTION_SHORT_MOMENTUM_PARAM` | 短期动量过滤 | 开或关 |
| `r` | `EXECUTION_R2_PARAM` | R2 趋势拟合过滤 | 关闭或阈值 0.3 到 0.6 |
| `ar` | `EXECUTION_ANNUAL_RETURN_PARAM` | 年化收益率过滤 | 开或关 |
| `ma` | `EXECUTION_MA_PARAM` | 均线过滤 | 开或关 |
| `v` | `EXECUTION_VOLUME_PARAM` | 成交量过滤 | 阈值 0.8 到 2.0 |
| `ls` | `EXECUTION_LOSS_PARAM` | 近三日跌幅过滤 | 开或关 |
| `fl` | `EXECUTION_FIXED_STOPLOSS` | 固定比例止损 | 开或关 |
| `slday` | `EXECUTION_SELL_COOLDOWN` | 卖出冷却期 | 开或关 |
| `fm` | `EXECUTION_FLITER_MARKET` | 动态池市场过滤 | 三个布尔位分别表示：过滤 A 股、过滤 QDII、过滤债基货币 |

本次测试覆盖了配置文件中的以下轮次：

- `round1a_switches`
- `round1b_switches`
- `round1c_close_Ashare`
- `round2_fine_tuning`
- `round2b_fine_tuning`
- `round2c_Ashare`

其中 `round1a` 和 `round1b` 偏开关测试，`round2` 和 `round2b` 偏核心参数微调，`round1c` 和 `round2c` 主要验证 A 股相关过滤对结果的影响。

## 各轮次最佳结果摘要

下面从 219 条完成记录中截取各轮次代表结果，重点看参数搜索过程中的方向性结论。

| 轮次 | 主要目的 | 最优代表 | Ann.Ret | MaxDD | Calmar | Sharpe | 观察 |
|---|---|---|---:|---:|---:|---:|---|
| `round1a` | 基础开关测试 | `ar✓ ls✓ fl- ma- st-` | 52.4% | 31.9% | 1.64 | 1.36 | 年化过滤和近三日跌幅过滤有价值，固定止损不一定需要打开 |
| `round1b` | 加入卖出冷却测试 | `ar✓ ls✓ fl✓ slday✓ ma- st-` | 62.0% | 30.3% | 2.05 | 1.73 | 相比 `round1a`，卖出冷却和固定止损组合明显改善收益回撤比 |
| `round1c` | A 股相关过滤扩展 | `ar✓ fl✓ ls✓ ma✓ st✓ fm=过滤A股+过滤债货` | 53.3% | 20.5% | 2.60 | 1.64 | 收益下降，但回撤显著降低，适合偏防守的版本 |
| `round2` | 核心参数微调 | `S[0,5] r=0.4 v=1.5` | 81.0% | 24.1% | 3.36 | 2.38 | 综合表现最好，收益和回撤都比较均衡 |
| `round2b` | 关闭年化收益率过滤后的微调 | `S[0,5] r=0.4 v=2.0` | 76.5% | 27.0% | 2.83 | 2.22 | 有收益弹性，但风险控制弱于 `round2` |
| `round2c` | A 股过滤/补充开关验证 | `S[0,5] r=0.4 v=1.2 fm=过滤A股+过滤债货` | 65.1% | 19.2% | 3.40 | 2.15 | Calmar 最高，说明过滤 A 股后回撤控制明显改善 |

## 代表组合截取

从完整结果中截取几组代表结果如下。这里不是按各轮最优逐一列出，而是选取能代表不同参数方向和年度特征的组合。括号内为该指标在 219 条结果中的排名；`Return`、`Calmar`、`Y*` 越高越靠前，`MaxDD` 越低越靠前。

| 轮次 | 参数摘要 | Return | Ann.Ret | MaxDD | Calmar | Y2023 | Y2024 | Y2025 | Y2026 | 代表性 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `round2` | `S[0,5] r=0.4 ar✓ v=1.2 ls✓ fl✓ slday✓ fm=过滤债货` | 534.6%（#1） | 83.5% | 25.0%（#18） | 3.34（#3） | 23.6%（#7） | 55.5%（#12） | 151.5%（#73） | 31.2%（#61） | 进攻型最优收益，总收益第 1 且 Calmar 第 3 |
| `round2c` | `S[0,5] r=0.4 ar✓ v=1.2 ls✓ fl✓ slday✓ fm=过滤A股+过滤债货` | 360.2%（#22） | 65.1% | 19.2%（#1） | 3.40（#1） | 4.9%（#104） | 74.8%（#1） | 97.1%（#182） | 27.4%（#103） | 过滤 A 股后的防守最优，回撤和 Calmar 均第 1 |
| `round2c` | `S[0,5] r=0.4 ar✓ v=1.2 ls✓ fl✓ slday✓ fm=过滤国际ETF+过滤债货` | 403.9%（#12） | 70.1% | 25.3%（#22） | 2.77（#11） | 24.7%（#5） | 13.6%（#111） | 177.1%（#20） | 28.3%（#91） | 过滤国际 ETF 后仍有收益弹性，2023 和 2025 靠前 |
| `round2b` | `S[0,5] r=0.3 v=1.2 ls✓ fl✓ slday✓ fm=过滤债货` | 412.9%（#11） | 71.1% | 28.9%（#67） | 2.46（#26） | 27.5%（#1） | 33.5%（#41） | 129.6%（#125） | 31.2%（#61） | 关闭年化过滤后的强势样本，2023 单年第 1 |
| `round1c` | `S[0,5] ls✓ fl✓ slday✓ fm=过滤A股+过滤债货` | 292.7%（#51） | 56.7% | 23.9%（#10） | 2.37（#33） | 13.0%（#52） | 45.2%（#19） | 86.9%（#200） | 28.0%（#96） | 早期 A 股过滤方向验证，未细调时回撤已进前 10 |
| `round1b` | `S[0,5] ar✓ ls✓ fl✓ slday✓ fm=过滤债货` | 334.1%（#32） | 62.0% | 30.3%（#87） | 2.05（#49） | 20.7%（#21） | 21.3%（#83） | 124.5%（#134） | 32.1%（#43） | 卖出冷却、年化过滤和止损开关基线，收益改善但仍弱于精细调参 |
| `round1a` | `S[0,5] ar✓ ls✓ fl✓ fm=过滤债货` | 271.0%（#65） | 53.8% | 34.0%（#138） | 1.59（#97） | 6.9%（#92） | 17.3%（#98） | 124.1%（#138） | 32.1%（#43） | 最早期开关测试基线，有收益弹性但风险控制较粗 |
| `round2` | `S[0,10] r=0.5 ar✓ v=0.8 ls✓ fl✓ slday✓ fm=过滤债货` | 168.2%（#200） | 38.3% | 38.3%（#187） | 1.00（#199） | -27.8%（#217） | -7.5%（#197） | 217.3%（#1） | 26.5%（#114） | 2025 单年冠军但整体较弱，说明不能只看单年收益 |
| `round2` | `S[0,8] ar✓ ls✓ fl✓ slday✓ fm=过滤债货` | 232.9%（#118） | 48.5% | 35.9%（#164） | 1.35（#140） | 0.4%（#125） | -13.9%（#212） | 161.4%（#48） | 47.3%（#1） | 2026 单年冠军但整体一般，代表年度偏科组合 |

## 结果分析

### 参数微调比单纯开关测试更关键

从 `round1b` 到 `round2`，提升非常明显。

`round1b` 代表结果为年化 62.0%、最大回撤 30.3%、Calmar 2.05；`round2` 代表结果提升到年化 81.0% 到 83.5%、最大回撤 24.1% 到 25.0%、Calmar 3.34 到 3.36。

这说明在这个策略上，核心参数 `S`、`r`、`v` 的组合，比单纯打开或关闭某些过滤器更重要。

### `S[0,5]` 是这一轮里最稳定的评分区间

排名靠前的组合高度集中在 `S[0,5]`。按 Calmar 排序，前 10 组全部是 `S[0,5]`，前 20 组里有 17 组是 `S[0,5]`。

从本轮结果看，`S[0,5]` 在收益弹性和过滤强度之间取得了较好的平衡。

### `r=0.4` 是当前最稳的中心值

R2 阈值方面，`r=0.4` 的稳定性最突出。按 Calmar 排序，前 10 组里有 8 组使用 `r=0.4`；`round2`、`round2b`、`round2c` 各自的最优代表也都落在 `r=0.4`。

不过这不代表 `r=0.4` 是唯一可行值。前列组合里也出现了 `r=0.3` 和关闭 R2 的结果，只是从当前样本看，`r=0.4` 的综合表现更集中。

### 成交量过滤的较优区间集中在 `v=1.2` 到 `v=2.0`

`round2` 的前几组代表结果分别是：

- `S[0,5] r=0.4 v=1.5`
- `S[0,5] r=0.4 v=1.2`
- `S[0,5] r=0.4 v=2.0`
- `S[0,5] r=0.3 v=2.0`

其中 `v=1.5` 的 Calmar 为 3.36，`v=1.2` 为 3.34，`v=2.0` 也达到 3.25。`round2b` 的最优代表同样是 `v=2.0`。所以当前结果显示 `v=1.2` 到 `v=2.0` 都进入了第一梯队。

### A 股过滤能明显降低回撤，但会牺牲收益

`round2c` 中 `S[0,5] r=0.4 v=1.2 fm=过滤A股+过滤债货` 的 Calmar 达到 3.40，最大回撤降到 19.2%，是当前表中 Calmar 最高的组合。

但代价也很清楚：与 `round2` 最优的年化 83.5%、最大回撤 25.0%、Calmar 3.34 相比，A 股过滤版本把最大回撤压低到 19.2%，Calmar 小幅提升到 3.40，但年化收益降到 65.1%。

因此它更像是偏防守版本，而不是收益最大版本。

与之相比，过滤国际 ETF 和债货的版本 `S[0,5] r=0.4 v=1.2 fm=过滤国际ETF+过滤债货`，年化收益为 70.1%，最大回撤为 25.3%，Calmar 为 2.77。它的收益弹性强于过滤 A 股版本，但回撤控制明显弱一些。

### 关闭年化收益率过滤后仍有收益弹性，但不占优

`round2b` 的当前最优代表是 `S[0,5] r=0.4 v=2.0`，年化 76.5%，最大回撤 27.0%，Calmar 2.83。这个结果比 `S[0,5] r=0.3 v=1.2` 的 Calmar 2.46 更好，说明关闭年化收益率过滤后并不是完全失效。

但它仍低于 `round2` 最优的 Calmar 3.36，也低于 `round2c` 最优的 Calmar 3.40。也就是说，关闭年化收益率过滤后仍有收益弹性，但从当前结果看，并不是风险收益比最优方向。

## 初步选择

如果只选一个进攻型版本，我会优先看：

```text
S[0,5], r=0.4, v=1.5
```

对应 `round2` 的当前最优代表，年化 81.0%，最大回撤 24.1%，Calmar 3.36。`v=1.2` 和 `v=2.0` 也在同一梯队，适合继续做敏感度验证。

如果更重视回撤控制，我会优先看：

```text
S[0,5], r=0.4, v=1.2, fm=过滤A股+过滤债货
```

对应 `round2c` 的 A 股过滤版本。它的年化收益低一些，但最大回撤降到 19.2%，Calmar 最高。

## 局限和后续验证

这组结果只能说明在 2023-01-01 到 2026-03-01 这个区间内，某些参数方向表现更好。它还不能直接说明这些参数就是最终最优。

后续至少还需要做几类验证：

1. 换更早的历史区间，看参数是否仍然有效。
2. 做样本外验证，避免只适配 2023 到 2026 这一段行情。
3. 对 `S`、`r`、`v` 做更细的敏感度测试。
4. 检查不同年份的持仓明细，确认收益不是来自极少数偶然交易。
5. 对动态池刷新周期、流动性门槛和市场类型过滤继续做交叉测试。

## 小结

这次案例说明，`backtest_executor` 的价值不只是自动提交回测，更重要的是把实验过程结构化了。

对于 `ETF_7star_opt_dynamic.py` 这类参数较多的策略，手工记录很容易遗漏参数上下文。通过 `etf_7star_opt_dynamic.yaml` 把参数空间和实验轮次固化下来之后，结果就能按轮次、按参数、按指标清楚地复盘。

从当前结果看，七星动态池策略的较优方向是：`S[0,5]` 的得分区间、`r=0.4` 左右的 R2 过滤、`v=1.2` 到 `v=2.0` 的成交量过滤，以及根据风险偏好选择是否过滤 A 股标的。

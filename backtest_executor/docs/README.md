# backtest_executor 文档

这里集中存放 `backtest_executor` 当前框架说明、规划文档和历史研究文章。

## 目录

- `planning/backtest_parameter_comparison_table_design.md`: 多参数回测结果对比表设计。
- `planning/yaml_parameter_optimization_framework_design.md`: YAML 驱动参数优化框架设计。
- `forum_articles/`: 面向 JQ 论坛或策略复盘发布的文章草稿。
- `../archive/README.md`: 旧回测系统说明，以及当前框架尚未覆盖的旧功能清单。

当前代码入口仍在 `backtest_executor/` 包根目录，旧程序代码位于 `backtest_executor/archive/`。

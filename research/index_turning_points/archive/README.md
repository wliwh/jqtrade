# 历史归档

本目录保存不再代表现行结论的旧代码和实验结果，默认只读。现役模块不得直接依赖这里的 Python 代码或文件布局；确需作为历史基线时，在 `adapters/` 增加带版本名称的显式适配器，并把新结果写入 `artifacts/` 的新版本目录。

当前归档：[`four_industry_width_v1/`](four_industry_width_v1/README.md)。

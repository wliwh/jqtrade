# 新高—新低广度60/120/250日分项拆分 V1

状态：阶段 E 冻结定义；定义先于首次评测落盘，评测后未修改。用于在完全相同的阈值和事件协议下，比较60、120、250日新高—新低净广度的独立效力；不改写已生成的三周期等权组合 V1。

## 输入与点时边界

- 输入使用已验收 `all_a_p1_inputs_v2_20120101_20260814` 的60/120/250日新高、新低计数、比例和各自有效分母；实际覆盖2012-01-04至2026-08-14。
- 日期 `t` 的股票池、前复权日内最高/最低价及历史极值只读取不晚于 `t` 的数据；新高、新低均以包含当日的对应回看窗口判断。
- 每个周期只使用自身有效分母；因历史不足、停牌、ST或价格缺失而不可比较的股票不进入分母。
- 三个周期从同一日期开始，前5个交易日因变化量不可得而保持不触发，不删除。
- 阈值完全继承已冻结的三周期等权组合V1，不根据任一周期的标签、收益或触发频率分别调整。

## 冻结公式

对 `N ∈ {60, 120, 250}`：

```text
net_N_t       = new_high_ratio_N_t - new_low_ratio_N_t
change_N_5d_t = net_N_t - net_N_(t-5)

top_N_t    = net_N_t >=  0.05 and change_N_5d_t <= -0.03
bottom_N_t = net_N_t <= -0.05 and change_N_5d_t >=  0.03
```

- 每个周期输出顶部、底部两条独立序列，共六条；`raw_value` 为该周期的新高—新低净比例。
- `universe_size` 使用点时全A成分数，`valid_count` 使用该周期自身的新高新低有效分母。
- 输出保留 `high_low_window`、新高/新低计数和比例、净计数和比例、5日变化、有效分母及变化可用状态。
- 信号内样本量预检只用于确认能够进入冻结推断：60日顶/底98/73段，120日48/43段，250日24/27段；预检没有读取顶底区域或指数未来收益。

## Episode 与比较

六条序列统一使用 [`events.py`](../../signals/events.py)：连续触发日合并为episode，onset为首个活跃日，capped confirmation固定为第2个活跃日，单日短段在看到退出日时确认，样本尾部不回填。

区域定位与5/10/20日OHLC评测使用同一个Stage D bundle，使六条序列共享一个全局FDR家族。比较时分别报告顶部/底部、预测/确认、strict/loose/window和各指数，不通过调阈值、改起点、删年份或只选择最好周期提高成绩。

## 结果入口

正式产物见 [`signal bundle`](../../artifacts/signals/new_high_low_period_decomposition_v1_20120104_20260814/) 和 [`stage_d_v1`](../../artifacts/evaluations/new_high_low_period_decomposition_v1_20120104_20260814__stage_d_v1/)；跨信号结论见 [`signal_backlog.md`](../signal_backlog.md#首次冻结评测总览)。

# ETF轮动策略研究(四)：高收益防御 vs 动量加速

本文旨在分析一种流行的ETF轮动策略，它的核心仍然是近期价格线性回归，与原策略有下面这些不同：

1. 交易时间改到开盘1小时后，将交易前那1分钟的价格拼接到历史价格序列中，并使用**加权线性回归**，赋予近期数据更高权重；
2. 大大扩展了ETF池，目前包含有国内宽基、行业ETF、债权、海外指数、商品指数等多样选择；
3. 添加了众多过滤措施，包括评分范围、近期大跌、均线、交易量、评分稳定性（R2）、短期收益等等过滤方法；
4. 增添日内止损模块。


```python
rules = [
    ['S', 'EXECUTION_SCORE_RANGE', [(0.0, float(x)) for x in (4,5,6,7,8,10)],\
     lambda x:'S'+('0' if x<10 else '')+'{:.1f}'.format(x[1])],
    ['ls', 'EXECUTION_LOSE_PARAM', [(False, 0.95), (True, 0.95)],\
     lambda x: 'ls'+'{:.2f}'.format(x[1])[2:] if x[0] else ''],
    ['ma', 'EXECUTION_MA_PARAM',  [(False, 20), (True, 20)], lambda x: 'ma' if x[0] else ''],
    ['v', 'EXECUTION_VOLUME_PARAM', [(False, 5, 0.6)] + [(True, 5, x) for x in (0.4, 0.6, 0.8, 1.0)],\
     lambda x: 'v{:.1f}'.format(x[1]).replace('.','') if x[0] else ''],
    ['r', 'EXECUTION_R2_PARAM', [(False, 0.4)] + [(True, t) for t in (0.4,0.5,0.6,0.8,1.0)],\
     lambda x: 'r{:.1f}'.format(x[1]).replace('.','') if x[0] else ''],
    ['st', 'EXECUTION_SHORT_MOMENTUM_PARAM', [(False, 10, 0.0), (True, 10, 0.0)], lambda x: 'st' if x[0] else ''],
    ['ar', 'EXECUTION_ANNUAL_RETURN_PARAM', [(False, 1.0), (True, 1.0)], lambda x:'ar' if x[0] else ''], 
    ['dl', 'EXECUTION_DAY_LIMIT_PARAM', [(True, 0.95), (False, 0.95)], lambda x: 'dl' if x[0] else '']
]
```

## 参数调优方案：基于参数相关性的智能搜索

### 参数分类

| 类型 | 参数 | 说明 |
|------|------|------|
| **范围项** | `S` (评分上限) | 控制入选标的的宽严程度 |
| **范围项** | `v` (成交量阈值) | 过滤异常放量标的 |
| **范围项** | `r` (R²稳定性) | 过滤动量不稳定的标的 |
| **开关项** | `ls` (近3日跌幅) | 默认关闭 |
| **开关项** | `ma` (均线过滤) | 默认关闭 |
| **开关项** | `st` (短期动量) | 默认关闭 |
| **开关项** | `ar` (年化收益) | 默认关闭 |
| **开关项** | `dl` (日内止损) | 默认开启 |

### 第一步：搜索最优的开关项

### 参数间相关性分析

| 参数对 | 相关性 | 原因 |
|--------|--------|------|
| `S` ↔ `r` | **强相关** | S 控制评分宽度，r 控制评分稳定性，两者联合决定标的筛选质量 |
| `S` ↔ `v` | 弱相关 | 成交量过滤独立于评分维度 |
| `v` ↔ `r` | 无关 | 分别作用于不同维度（量/价） |

### 搜索步骤

**子空间 A：联合搜索 S × r（核心参数对）**

由于 `S` 和 `r` 强相关，必须联合搜索。固定 `v` 为默认关闭，`dl`开启：

- S 候选值：4, 5, 6, 7, 8, 10（6个）
- r 候选值：off, 0.4, 0.5, 0.6, 0.7（5个）
- 组合数：**6 × 5 = 30 组**

从结果中选出 Calmar Ratio（总收益/最大回撤）最优的 S\* 和 r\*。

**子空间 B：单独搜索 v（成交量过滤）**

固定 S=S\*, r=r\*，搜索最优的成交量阈值：

- v 候选值：off, 0.4, 0.6, 0.8, 1.0（5个）
- 组合数：**5 组**

**总计：35 组**（相比全量笛卡尔积 180 组，缩减 80%）

### 评价指标

优先使用 **Calmar Ratio**（年化收益 / 最大回撤），因为该策略的过滤器本质上是用交易频率换取低回撤，Calmar Ratio 最能体现过滤器是否起到了正向作用。

### 参数高原验证

在选出最优参数后，检查其邻近参数的表现：
- 若 S\*=6，检查 S=5 和 S=7 的表现是否也接近
- 若 r\*=0.5，检查 r=0.4 和 r=0.6 的表现是否也接近
- 如果邻近参数表现骤降，说明该参数是"孤岛参数"，存在过拟合风险